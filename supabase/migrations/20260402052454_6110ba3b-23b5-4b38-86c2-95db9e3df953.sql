ALTER TABLE public.profiles DROP CONSTRAINT profiles_status_plano_check;
ALTER TABLE public.profiles ADD CONSTRAINT profiles_status_plano_check CHECK (status_plano IS NULL OR status_plano IN ('ativo', 'inativo', 'trial', 'expirado', 'pendente_pagamento', 'pendente_aprovacao', 'rejeitado'));